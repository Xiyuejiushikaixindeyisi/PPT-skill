# 通用大模型 PPT 生成 · 高密度工程流程图规范(粘贴即用版)

> 用法:把本文件整段贴给任意大模型,再在末尾补上"页面内容"。适用于无法执行代码的模型
> (T3);若模型能生成并运行 Python,建议改用 skill 的 T1/T2 脚本以获得自动质检闭环。
> 需要非默认品牌时,替换第 2 节 tokens 的值即可,其余不动。

## 0. 总原则(最高优先级)
面向企业内部评审的【工程满版风】,非咨询留白风:
- 每页以【一张复杂流程图/架构图/甘特图】为主体,主图占页面高度 ≥70%;
- 不允许出现任何 > 1.2″×1.2″ 的连续空白区(页边距除外);
- 文字尽量装进图形节点里,独立段落文字越少越好;
- 密而不乱:对齐网格、间距一致、连线不穿字、方框文字不溢出。

## 1. 交付与环境
- 用 python-pptx 生成可下载的 .pptx。
- 16:9(13.333″×7.5″)| 纯白底 | 微软雅黑全篇统一(回退:Noto Sans CJK SC / PingFang SC / SimHei)。
- 页边距 0.51″;页标题 20pt 加粗黑色、左对齐,位于标题带 (x5.5%, y1.7%, 宽89%, 高8%);标题下即进入主图。
- 每页右下角放 logo(宽 1.5″,保持比例,不遮内容)。
- 禁止:页眉小字、底部深色条、标题下划线、装饰色条。

## 2. 品牌 Tokens(默认部门扁平蓝,可替换)
| 角色 | 色值 | 用途 |
|---|---|---|
| title/text | #000000 | 页标题与正文文字(黑) |
| accent | #2683C6 | 主视觉蓝:强调填充、连线、当前阶段、序号块 |
| container | #D7EAFE | 一级浅底容器、泳道底、已完成甘特条 |
| nested | #CFE0FE | 容器子块、二级浅蓝 |
| navy | #04194C | 第二品牌色:章节页左栏、序号圆点 |
| chevron_mid | #558ED5 | chevron 中段、架构中间层、回流箭头 |
| neutral | #F2F2F2 | 中性块、未开始甘特条 |
| muted #808080 | 小标签、图注、图例 |
| border_gray #BFBFBF / gray_line #D9D9D9 | 卡片细描边 / 分隔线表格线 |
| red #C00000 / amber #FFC000 / pink #FFC8D3 | 风险·今日线·红描边圈注框 / 里程碑高亮 / 第二分组色 |
| blue_text #0070C0 | 行内蓝色术语强调 |
蓝色是唯一的第一分组色;需要更多分组色按 pink → amber 顺序取,不引入新色。
扁平风:无阴影、无渐变;容器一律直角,仅 ≤1.1×0.5in 的小标签 chip 允许圆角。形状平铺不打组。
字号梯度(pt):封面标题 40 → 节标题 28 → 页标题 20 → 容器标题 14 → 节点标题/正文 12
→ 次级说明 10 → 图注/页脚 9;**全篇最小 8pt(硬约束),正文保持 12pt,9–10pt 只给图注**。
密度靠结构(嵌套/网格/chip/箭头)与短文案(节点单行 ≤8–10 个汉字)达成,禁止缩小字号换密度;
装不下就精简措辞、拆两行或加宽方框。

## 2.5 标准页面框架(按序组织全篇)
封面 → 目录 → [节标题 → 内容页×N] → 末尾页(≤6 页可省目录与节标题)。
- 封面:主标题 40pt 加粗黑 (x5.4%,y8.8%,宽80%),上方可加 1.2″×0.06″ accent 色条;
  副标题 14pt (y56.4%);落款 9pt 灰 (y91%);logo 右下。
- 目录:页标题带 + 每条目 = accent 底白字序号块(01/02…)+ 14pt 黑条目文字 + 浅灰下分隔线。
- 节标题:左侧 37.5% 全高 navy 藏青栏(可叠图)+ 栏内 60pt 加粗白大序号;右侧竖灰分隔线
  + 议程列表(●圆点 + "0X" + 节标题 20pt 加粗;当前节 navy 色、其余 #808080 灰)。
- 内容页:标题带 20pt 加粗黑 + 页面家具(标题下通栏灰线 #BFBFBF 0.75pt @y9.8%、
  左上角蓝色小图标、左下 "page N" 8pt 灰页码、右下 logo)+ 可选分组导语标签
  (标题下浅蓝底 12pt 加粗)+ 主图区(y≈12%→94%,高度≥70%)。标题写结论式长句
  ("XX 通过 XX 实现 XX",20 字以上为常态),不写名词短语("XX 介绍")。
- 末尾页:居中 40pt 加粗"谢 谢" + 可选 14pt 灰副行 + logo 右下。

## 3. 母题库(每页选 1 主 + ≤2 辅)
**所有文本框/方框/标签 chip 一律用直角矩形(不得使用圆角矩形)**,1pt 描边;
连线用 accent 直角箭头;分支用小圆点接头。圆形仅用作序号/图标标记,菱形仅用作判断与里程碑。
- A 横向嵌套泳道流:左竖排场景标签→主链节点,链中嵌浅蓝容器(含子步骤),容器间弧形箭头表循环,右端收敛判断/结论。(Agent 多 skill 协作)
- B 阶段 Chevron:顶部咬合箭头阶段条(蓝渐变),每阶段下挂子步骤方框,允许分支汇入;左竖排泳道标签。(生命周期)
- C 数据示例面板:竖向小标题面板,内嵌迷你表(深蓝表头+斑马纹)、SQL 片段、输入→输出。(用真实数据举证,密度最高)
- D 竖向管线:竖向步骤头,每步内嵌并列子块,步骤间下箭头,侧边旁注。(单功能处理管线)
- E 甘特/时间计划:顶部时间刻度+竖网格线;左任务列(可分组);状态色条(已完成 container/进行中 accent/未开始 neutral);◆里程碑(title);今日虚线(accent);底部图例。缺刻度或图例即不合格。
- F 分层架构:自上而下全宽层带(交替 container/nested/neutral);左竖排层名;层内等宽模块网格;右侧可竖向贯穿"治理/支撑"条;层间上下箭头表调用。
- G 泳道协作:每角色一道(道头 title 色),节点跨道连线表交接,菱形=判断,回流箭头=返工。
- H 时序交互:顶部参与方头(title 色)+竖向虚线生命线;编号消息箭头(请求 accent、返回 chevron_mid 反向),箭头上短标注;生命线上窄激活条(container 色);循环/分支用细边框容器+角标 chip。(接口时序/tool-call 往返)
- I 部署拓扑:2–4 个分区容器(container/nested/neutral 交替,主分区 border_blue 描边+title 色标题条),区内节点方框成网格;跨区连线标协议/端口 chip(如 "HTTPS 443");节点数 chip("×XX 节点");底部分区图例。
- J 状态机:实心圆=初态,白底 border_blue 方框=状态(状态名+一行动作),迁移箭头挂事件 chip(nested 底 accent 字),≥1 条 chevron_mid 回流箭头=驳回/返工,container 色=终态;守卫分支用菱形。状态按网格排(≤2 行),不摆圆圈。(工单/订单状态流转)
- K 四象限矩阵:整幅方形绘图区,中心十字细轴(border_gray)+轴端标签;四象限交替浅底色+角部象限标题;条目按打分定位成 chip(重点项 accent 描边);右侧结论列(先做哪个象限+KPI chips)。(价值×成本/紧急×重要/风险矩阵)
- L As-Is→To-Be 对比:左 neutral 容器=现状、右 container+border_blue=目标,道头分别 muted/title 色;左右行数一致、逐行镜像对齐;中央大箭头(accent)+2–3 个改造要点 chip;底部量化收益条(XX天→XX天)。禁止两侧各写一段自由文字。

内容→母题速查:有起止时间→E;分层平台→F;多角色交接→G;Agent 迭代→A;阶段生命周期→B;单功能管线→D;系统间时序调用→H;部署/网络分区→I;单实体状态流转(带事件/驳回)→J;两轴打分分布→K;现状 vs 目标对比→L;需用数据举证→C(通用辅助,优先选结构性主母题)。

## 4. 防空白兜底(必须执行,按序直到空白≤1.2″)
① 扩大主图(加注释/子块/拉伸行高列宽铺满);② 加辅助层(图例、状态说明、KPI chips、风险/依赖旁注、量化胶囊如"链路环节 XX+");③ 加迷你面板(母题 C 小表或输入→输出示例);④ 仍不适用才允许小面积留白(≤1.2″)。禁止用放大字号或拉大间距假装填满。

## 5. 成图前自检(逐条核对,过线才交付)
1. 主图高度 ≥ 页面 70%?无 >1.2″×1.2″ 空白块?内容饱满?
2. 甘特:时间刻度均匀、任务条与刻度对齐、图例齐全、里程碑/今日线正确?
3. 架构:层带全宽、层内模块等宽对齐、层间箭头方向正确?
4. 所有方框文字不溢出、连线不穿字、箭头方向正确?**全部文字 ≥8pt(逐字检查,一处小于即不合格;正文 12pt、图注最小 9pt)**?
5. 页标题 20pt 加粗黑;**所有方框均为直角矩形(无一处圆角),无阴影无渐变**;右下角 logo 未变形未遮挡;无页眉、无底部深色条、无标题下划线?
6. 每个内容页图形元素充足(≥20 个,目标 30+),非"三卡片+大留白"?
7. 导出可下载 .pptx。

## 6. 页面内容(用户填写)
在此粘贴每页标题与要点,并标注每页想用的母题(不标则由模型按第 3 节速查自动路由)。
示例:
- P1(母题 D + G):挑战…痛点/解法/效果…
- P2(母题 C 表 + E 甘特):对标表…推广节奏…
- P3(母题 F):输入层/工作流层/配置层/输出层…
